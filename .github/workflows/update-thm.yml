name: Update THM Stats
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Update Badge
        uses: grzywacz/tryhackme-stats-card-action@v1
        with:
          username: r9ruben
